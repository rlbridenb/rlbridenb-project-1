# TODO & Roadmap

### Currently Working On

- Extracting UI components out of `app.component.ts` (e.g., plane-card, menu) instead of housing everything on

### Roadmap

- Increase text size, display airport code + location instead of just code (e.g., MCO --> Orlando)
- Replace default "No aircraft nearby" view with local weather conditions (temperature, precipitation chance, wind direction + speed, etc.) when no aircraft are within the boundary
- Potentially add theme for sunrise/sunset
